import Logo from '../Logo/Logo';
import Nav from '../Nav/Nav';
import './footer.css';

const Footer = () => {
  return (
    <footer className='footer'>
      <Logo />
      <Nav />
    </footer>
  );
};

export default Footer;
